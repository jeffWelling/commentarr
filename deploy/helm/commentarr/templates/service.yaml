apiVersion: v1
kind: Service
metadata:
  name: {{ include "commentarr.fullname" . }}
  labels:
    {{- include "commentarr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commentarr.selectorLabels" . | nindent 4 }}
